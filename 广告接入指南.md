# 微信小游戏广告接入完整指南

## 📋 准备工作

### 1. 注册微信小程序账号

1. 访问 https://mp.weixin.qq.com/
2. 点击"立即注册"
3. 选择账号类型：**小游戏**
4. 填写信息并完成验证

### 2. 获取 AppID

1. 登录微信小程序后台
2. 进入"开发" → "开发管理" → "开发设置"
3. 复制你的 **AppID**

### 3. 开通广告功能

⚠️ **重要限制：**
- **个人主体**：广告功能受限，部分广告类型不可用
- **企业主体**：可使用全部广告类型，推荐

**开通步骤：**
1. 登录微信小程序后台
2. 进入"流量主"（需要先满足条件）
3. 点击"开通流量主"

**流量主开通条件：**
- 累计独立访客 ≥ 1000 人
- 无违规记录

## 🎯 创建广告位

### 步骤 1：进入广告管理

1. 登录微信小程序后台
2. 点击左侧"流量主"
3. 点击"广告位" → "添加广告位"

### 步骤 2：创建激励视频广告

```
广告类型：激励视频广告
广告名称：游戏复活广告
```

创建后你会得到一个 **广告单元 ID**，格式类似：`adxxxxxxxxxxxxxxxx`

### 步骤 3：创建其他广告位

建议创建以下广告位：

| 广告类型 | 名称建议 | 使用场景 |
|----------|----------|----------|
| 激励视频 | 游戏复活广告 | 游戏结束复活 |
| 激励视频 | 额外步数广告 | 获取额外步数 |
| 插屏广告 | 关卡插屏广告 | 关卡切换时 |
| Banner | 底部 Banner | 主界面底部 |

## 🔧 配置项目

### 1. 填写 AppID

编辑 `project.config.json`：

```json
{
  "appid": "你的 AppID 在这里"
}
```

### 2. 填写广告单元 ID

编辑 `js/ad.js`，找到 `adUnitIds` 配置：

```javascript
this.adUnitIds = {
  rewarded: 'adxxxxxxxxxxxxxxxx',      // 激励视频广告 ID
  interstitial: 'adxxxxxxxxxxxxxxxx',  // 插屏广告 ID
  banner: 'adxxxxxxxxxxxxxxxx',        // Banner 广告 ID
  grid: 'adxxxxxxxxxxxxxxxx'           // 格子广告 ID
}
```

### 3. 测试广告 ID

微信提供测试广告 ID，开发阶段可以使用：

```javascript
// 测试用激励视频广告 ID（示例，实际请查阅官方文档）
this.adUnitIds = {
  rewarded: 'adca-xxxxx-test',
  interstitial: 'adca-xxxxx-test',
  banner: 'adca-xxxxx-test'
}
```

## 📱 广告展示策略

### 激励视频广告（推荐⭐）

**最佳展示时机：**
- 游戏结束时 → "看广告复活"
- 步数用尽时 → "看广告 +5 步"
- 想要双倍奖励时 → "看广告双倍分数"

**代码示例：**
```javascript
// 游戏结束时显示复活按钮
if (this.state === 'gameover') {
  // 绘制复活按钮
  this.ctx.fillText('看广告复活', this.width / 2, btnY)
}

// 点击时调用
adManager.showRewardedAd()

// 广告回调
adManager.setCallback('onReward', (adType) => {
  if (adType === 'rewarded') {
    game.useAdReward('revive') // 复活
  }
})
```

### 插屏广告

**最佳展示时机：**
- 完成一个关卡后
- 游戏结束后（在复活选项之前）
- 返回主菜单时

**注意：** 插屏广告不要过于频繁，建议每 2-3 次展示一次

**代码示例：**
```javascript
// 关卡结束时
levelComplete() {
  // 50% 概率展示插屏广告
  if (Math.random() < 0.5) {
    adManager.showInterstitialAd()
  }
  // 进入下一关
  this.nextLevel()
}
```

### Banner 广告

**最佳位置：** 屏幕底部，不遮挡游戏内容

**代码示例：**
```javascript
// 游戏启动时显示
adManager.showBannerAd()

// 游戏过程中可以隐藏
adManager.hideBannerAd()
```

## 💰 广告收益说明

### 收益计算

微信广告收益基于 **eCPM**（每千次展示收益）：

| 广告类型 | 预估 eCPM | 说明 |
|----------|-----------|------|
| 激励视频 | ¥20-80 | 收益最高，用户主动观看 |
| 插屏广告 | ¥10-40 | 收益中等 |
| Banner | ¥2-10 | 收益较低但稳定 |

### 收益示例

假设你的游戏有 1000 日活用户：

```
激励视频广告：
- 30% 用户观看广告 = 300 次/天
- eCPM = ¥40
- 日收益 = 300 × 40 / 1000 = ¥12/天
- 月收益 ≈ ¥360/月

插屏广告：
- 50% 展示率 = 500 次/天
- eCPM = ¥20
- 日收益 = 500 × 20 / 1000 = ¥10/天
- 月收益 ≈ ¥300/月

合计月收益：约 ¥660/月
```

**注意：** 实际收益取决于用户质量、广告主出价等因素

## ⚠️ 注意事项

### 1. 广告规范

- ❌ 不要诱导点击（"点击广告支持我们"）
- ❌ 不要遮挡广告内容
- ❌ 不要修改广告样式
- ✅ 给用户选择权（特别是激励视频）
- ✅ 合理控制展示频率

### 2. 违规风险

以下行为可能导致封禁：
- 刷广告（自己点击自己的广告）
- 诱导用户点击
- 广告展示过于频繁
- 虚假奖励承诺

### 3. 审核流程

- 首次提交广告功能需要审核（1-3 个工作日）
- 确保游戏内容合规
- 确保广告展示合理

## 🧪 测试流程

### 1. 开发测试

```javascript
// 开发环境使用测试广告 ID
const isDev = true

if (isDev) {
  adManager.updateAdUnitIds({
    rewarded: '测试广告 ID',
    interstitial: '测试广告 ID'
  })
}
```

### 2. 真机测试

1. 微信开发者工具 → "真机调试"
2. 扫码在手机上测试
3. 检查广告是否正常显示

### 3. 体验版测试

1. 上传代码到微信后台
2. 设置为"体验版"
3. 邀请测试人员体验

## 📊 数据监控

### 微信小程序后台数据

登录后台可查看：
- 广告展示次数
- 广告点击次数
- 广告收益
- 用户留存率

### 优化建议

1. **A/B 测试**：测试不同广告展示频率
2. **用户分层**：新用户减少广告，老用户适当增加
3. **时段分析**：高峰时段优化广告策略

## 🔗 相关资源

- 微信小游戏官方文档：https://developers.weixin.qq.com/minigame/dev/
- 广告组件文档：https://developers.weixin.qq.com/minigame/dev/guide/basis/ad.html
- 流量主文档：https://developers.weixin.qq.com/minigame/dev/guide/basis/flow.html
- 广告规范：https://developers.weixin.qq.com/minigame/dev/guide/basis/ad-spec.html

---

**祝你游戏大卖！💰**
