# 🚀 快速开始 - 5 分钟上线你的微信小游戏

## 第一步：准备工作（2 分钟）

### 1.1 注册小程序账号
```
访问：https://mp.weixin.qq.com/
点击"立即注册" → 选择"小游戏"
```

### 1.2 获取 AppID
```
登录后台 → 开发 → 开发管理 → 开发设置
复制 AppID（类似：wx1234567890abcdef）
```

## 第二步：配置项目（1 分钟）

### 2.1 修改 AppID

打开 `project.config.json`，找到这一行：

```json
"appid": "YOUR_APPID_HERE"
```

改成你的 AppID：

```json
"appid": "wx1234567890abcdef"
```

### 2.2 下载微信开发者工具

```
访问：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
下载并安装
```

## 第三步：导入项目（1 分钟）

1. 打开微信开发者工具
2. 点击"+" 添加项目
3. 选择项目目录：`/root/.openclaw/workspace/wechat-game`
4. 填入你的 AppID
5. 点击"导入"

## 第四步：运行测试（1 分钟）

1. 点击工具栏"编译"按钮
2. 在模拟器中测试游戏
3. 点击宝石进行消除
4. 测试广告功能（需要配置广告 ID）

## 第五步：配置广告（可选）

### 5.1 开通流量主

```
小程序后台 → 流量主 → 开通
（需要 1000 独立访客后才能开通）
```

### 5.2 创建广告位

```
流量主 → 广告位 → 添加广告位
创建激励视频广告、插屏广告等
```

### 5.3 填写广告 ID

打开 `js/ad.js`，找到：

```javascript
this.adUnitIds = {
  rewarded: '',
  interstitial: '',
  banner: '',
  grid: ''
}
```

填入你的广告单元 ID

## 第六步：上传发布

### 6.1 上传代码

1. 微信开发者工具 → 点击"上传"
2. 填写版本号和备注
3. 点击"上传"

### 6.2 提交审核

```
小程序后台 → 版本管理 → 开发版本
点击"提交审核"
```

### 6.3 发布上线

审核通过后：
```
小程序后台 → 版本管理 → 审核版本
点击"发布"
```

## 📁 项目文件说明

```
wechat-game/
├── game.json              # 游戏配置（不用改）
├── game.js                # 游戏入口（不用改）
├── project.config.json    # ⚠️ 需要填 AppID
├── js/
│   ├── main.js            # 入口文件（不用改）
│   ├── game.js            # 游戏逻辑（可以修改玩法）
│   └── ad.js              # ⚠️ 需要填广告 ID
├── images/                # 图片资源（可以替换）
├── README.md              # 项目说明
├── 广告接入指南.md         # 详细广告教程
└── 快速开始.md            # 本文件
```

## 🎮 游戏玩法

1. **点击** 宝石选中
2. **点击相邻** 宝石交换位置
3. **3 个及以上** 相同颜色连成一线消除
4. **步数用完** 前获得更高分数
5. **看广告** 可以复活或获得额外步数

## 🛠️ 自定义修改

### 修改宝石颜色

编辑 `js/game.js`，找到 `gemColors`：

```javascript
this.gemColors = [
  '#FF6B6B', // 红
  '#4ECDC4', // 青
  '#45B7D1', // 蓝
  '#96CEB4', // 绿
  '#FFEAA7', // 黄
  '#DDA0DD'  // 紫
]
```

### 修改网格大小

编辑 `js/game.js`，找到 `gridSize`：

```javascript
this.gridSize = 8  // 改成 6 或 10
```

### 修改初始步数

编辑 `js/game.js`，找到 `start()` 方法：

```javascript
this.moves = 20  // 改成你想要的步数
```

## ❓ 常见问题

### Q: 编译报错 "AppID 无效"
A: 检查 `project.config.json` 中的 AppID 是否正确

### Q: 广告不显示
A: 
- 检查广告 ID 是否填写正确
- 确保已开通流量主
- 测试阶段可用测试广告 ID

### Q: 如何替换图片资源
A: 将图片放入 `images/` 目录，在代码中引用

### Q: 审核不通过怎么办
A: 根据审核反馈修改，常见问题：
- 广告展示过于频繁
- 游戏内容不完整
- 隐私政策缺失

## 📞 需要帮助？

- 微信小游戏论坛：https://developers.weixin.qq.com/community/develop/mixflow
- 本文档位置：`/root/.openclaw/workspace/wechat-game/`

---

**祝你开发顺利！🎉**
